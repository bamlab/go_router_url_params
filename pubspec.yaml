name: go_router_url_params
description: "Easily keep your state in the url when using go_router in Flutter"
version: 0.0.1
homepage: https://github.com/bamlab/go_router_url_params
repository: https://github.com/bamlab/go_router_url_params

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ">=14.7.1 <18.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
